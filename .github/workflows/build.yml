name: Build APK

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # ========== 关键修复：手动下载完整的 Gradle Wrapper ==========
      - name: Fix Gradle Wrapper (download jar)
        run: |
          mkdir -p gradle/wrapper
          # 下载官方 gradle-wrapper.jar（8.4 版本）
          curl -L https://raw.githubusercontent.com/gradle/gradle/v8.4.0/gradle/wrapper/gradle-wrapper.jar -o gradle/wrapper/gradle-wrapper.jar
          # 生成 gradlew 启动脚本
          curl -L https://raw.githubusercontent.com/gradle/gradle/v8.4.0/gradlew -o gradlew
          chmod +x gradlew
          # 写入正确的 properties
          echo "distributionBase=GRADLE_USER_HOME" > gradle/wrapper/gradle-wrapper.properties
          echo "distributionPath=wrapper/dists" >> gradle/wrapper/gradle-wrapper.properties
          echo "distributionUrl=https\://services.gradle.org/distributions/gradle-8.4-bin.zip" >> gradle/wrapper/gradle-wrapper.properties
          echo "zipStoreBase=GRADLE_USER_HOME" >> gradle/wrapper/gradle-wrapper.properties
          echo "zipStorePath=wrapper/dists" >> gradle/wrapper/gradle-wrapper.properties

      # ========== 验证 wrapper 是否存在（防止漏网） ==========
      - name: Verify wrapper
        run: |
          ls -la gradle/wrapper/
          ./gradlew --version

      # ========== 正式构建 ==========
      - name: Build APK
        run: ./gradlew assembleDebug --stacktrace

      # ========== 上传成果 ==========
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/*.apk
